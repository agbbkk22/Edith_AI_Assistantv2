export function generateResponse(query: string): string {
  // This is a simple mock response. In a real app, you'd integrate with an AI service
  return `I understand you're asking about "${query}". As your business assistant, I'm here to help. What specific details would you like to know?`;
}